To run:
1. create a new virtualenv using python
2. run pip install -r requirements.txt in the new virtualenv
3. start the redis server at port 6379
4. execute newvirtualenv/bin/python main.py to run the application
5. Open http://localhost:5001/ in your browser once the server is running
